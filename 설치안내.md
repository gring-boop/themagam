# TheMagam 설치 안내

벨사탕 작업실과 **데이터를 완전히 나누기로** 했으므로, 파이어베이스를 새로 하나 만들어야 합니다.
지금 파일에 들어 있는 설정은 벨사탕 것이라, 그대로 올리면 두 방이 같은 방이 됩니다.

순서는 **① 파이어베이스 만들기 → ② 설정 갈아끼우기 → ③ 보안 규칙 넣기 → ④ 깃허브에 올리기** 입니다.

---

## ① 파이어베이스 만들기

1. <https://console.firebase.google.com> 접속 → 구글 로그인
2. **프로젝트 만들기** → 이름 `themagam` → 애널리틱스는 꺼도 됩니다 → 만들기
3. 왼쪽 메뉴 **빌드 → Realtime Database** → **데이터베이스 만들기**
   - 위치는 **싱가포르(asia-southeast1)** 를 고르세요. 한국에서 가장 빠릅니다.
   - 보안 규칙은 일단 **테스트 모드**로 시작 (③에서 제대로 바꿉니다)
4. 왼쪽 위 **⚙️ → 프로젝트 설정** → 아래로 스크롤 → **내 앱** → **웹 `</>`** 아이콘
5. 앱 닉네임 `themagam` 입력 → 등록
6. 화면에 `firebaseConfig = { ... }` 코드가 나옵니다. **이 부분을 복사**하세요.

---

## ② 설정 갈아끼우기

`script_core.js` 를 메모장이나 텍스트 편집기로 엽니다.
39번째 줄쯤에 이런 덩어리가 있습니다.

```js
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "writer-chat.firebaseapp.com",
  databaseURL: "https://writer-chat-default-rtdb...",
  ...
};
```

**중괄호 안쪽을 ①에서 복사한 것으로 통째로 바꾸세요.** `const firebaseConfig = {` 와 `};` 는 그대로 둡니다.

> `databaseURL` 이 목록에 없으면 직접 넣어야 합니다.
> Realtime Database 화면 맨 위에 `https://...firebasedatabase.app` 주소가 있어요.

저장하면 끝입니다.

---

## ②-B 로그인 켜기 ← **새로 추가된 단계, 안 하면 아무도 못 들어옵니다**

이제 필명과 **비밀번호**로 들어옵니다. 이걸 쓰려면 파이어베이스에서 스위치 하나를 켜야 해요.

1. 파이어베이스 콘솔 → 왼쪽 메뉴 **빌드 → Authentication**
2. **시작하기** 버튼
3. 로그인 방법 목록에서 **이메일/비밀번호** 를 고름
4. 맨 위 **사용 설정** 스위치를 **켜기** (아래 "이메일 링크"는 끈 채로 두세요)
5. **저장**

> 이 단계를 빼먹으면 입장할 때 "파이어베이스에서 이메일/비밀번호 로그인을 켜야 해요" 라고 뜹니다.

### 이메일을 안 받는데 왜 이메일 로그인인가요

파이어베이스가 이메일을 요구해서, **필명을 가짜 주소로 바꿔서** 씁니다.

    콩  →  n5f435b@themagam.local

메일이 오가지 않는 주소예요. 파이어베이스가 사람을 구분하는 열쇠로만 씁니다.
그래서 **Authentication 목록에 이상한 주소들이 쌓이는 게 정상**입니다.

### 비밀번호를 잊은 사람이 생기면

메일이 진짜가 아니라서 "재설정 메일 보내기"가 안 됩니다. **방장이 직접** 처리해야 해요.

- 콘솔 → **Authentication → Users** 목록에서 그 사람 줄의 **⋮ → 비밀번호 재설정** 은 못 씁니다.
- 대신 **⋮ → 사용자 삭제** 로 계정을 지우세요. 그러면 그 사람이 새 비밀번호로 다시 만들 수 있습니다.
- 계정을 지워도 **채팅·기록·펫은 그대로 남습니다.** 다른 곳에 저장돼 있어요.
- 단, **Realtime Database → nickOwner → 그 필명** 도 같이 지워야 합니다. 안 지우면 옛 계정 번호가 남아 있어서 새 계정이 그 필명을 못 씁니다. **둘 다 지우기**를 기억해 주세요.

### 필명은 먼저 온 사람이 임자

누군가 어떤 필명으로 처음 들어오는 순간, 서버에 `nickOwner/필명 = 그 사람` 도장이 찍힙니다.
이 도장은 **아무도 덮어쓸 수 없게** 규칙으로 막혀 있어요. 그 뒤로 같은 필명으로 들어오려면 그 비밀번호를 알아야 합니다.

---

## ③ 보안 규칙 넣기

**이 단계를 건너뛰지 마세요.** 테스트 모드는 30일 뒤 잠기고, 그 전까지는 **누구나 무엇이든 지울 수 있습니다.**

파이어베이스 콘솔 → **Realtime Database → 규칙** 탭에서 **칸 안을 전부 지우고**, 함께 넣어둔 **`보안규칙.json`** 의 내용을 붙여넣고 **게시**를 누르세요.

> **⚠️ 앞서 드린 규칙은 경로 넷을 빠뜨렸습니다.** `attendance`(출석), `achievementOverrides`(업적 테스트), `reactions`(채팅 반응), `chatMeta` 가 없었어요.
>
> 규칙에 없는 경로는 파이어베이스가 **조용히 거절합니다.** 오류가 화면에 안 뜨고 저장만 안 되니, "기능이 안 먹는다"로 보입니다. 실제로 출석 기록과 업적이 그래서 안 됐습니다. `보안규칙.json` 으로 다시 게시해 주세요.

이 규칙이 막아주는 것과 못 막는 것을 정확히 적어둡니다.

**막아줍니다** — 정의되지 않은 경로에 아무 데이터나 쓰는 것, 그리고 30일 뒤 테스트 모드가 잠기는 문제.

**이제 막아줍니다** — 예전에는 못 막던 "남의 필명으로 들어와 프로필·투두를 덮어쓰기"가 막혔습니다.
새 규칙은 `users/필명` 과 `status/필명` 을 **도장 주인만** 쓸 수 있게 하고, 나머지도 전부 **로그인한 사람만** 쓸 수 있게 합니다.

**여전히 못 막습니다** — 로그인한 사람이 채팅을 남의 이름으로 보내는 것 같은 세밀한 부분. 여기까지 막으려면 메시지마다 검사를 붙여야 해서, 필요해지면 그때 하시죠.

---

## ④ 관리자 PIN 바꾸기

`script_realtime.js` 에서 `ADMIN_PIN` 을 찾아 원하는 숫자로 바꾸세요.

```js
const ADMIN_PIN = "1009";
```

다만 이건 **실수 방지용**이지 잠금장치가 아닙니다. 코드가 공개돼 있어 누구나 읽을 수 있고, 개발자도구로 건너뛸 수도 있어요. 관리자 기능이 하는 일이 "히스토리 노출 설정"과 "업적 테스트" 정도라 큰 문제는 아니지만, 알고 쓰시는 게 좋습니다.

---

## ④-B 공용 뽀모 버튼

뽀모도로 칸 아래에 **🪟 공용 뽀모** 버튼이 생겼습니다. 누르면 마끝마 공용 뽀모방이 **새 창**으로 열려요.

    https://magammm1009.github.io/mmaapomopomo/

방 안에 끼워 넣지 않고 새 창으로 연 이유가 있습니다. 남의 사이트를 창 안에 넣으면(iframe) **소리와 알림이 브라우저에 막히고**, 그쪽에서 설정을 바꾸면 이쪽이 조용히 깨집니다. 새 창이 훨씬 튼튼해요.

이 방의 자체 뽀모는 그대로 두었습니다. 혼자 돌릴 때 쓰시고, 다 같이 맞출 때는 공용 뽀모를 쓰시면 됩니다.

---

## ⑤ 깃허브에 올리기

벨사탕 방과 **다른 저장소**를 새로 만들기로 하셨죠.

1. github.com → **＋ → New repository** → 이름 `themagam` → **Public** → Create
2. **uploading an existing file** → 이 폴더 안의 파일을 **전부** 끌어놓기 (`icons` 폴더 포함)
3. **Commit changes**
4. **Settings → Pages** → Branch `main` / `(root)` → **Save**
5. 1~2분 뒤 주소가 나옵니다 → `https://아이디.github.io/themagam/`

올리지 않아도 되는 파일: `build-single.py`, `checks.js`, `설치안내.md`, `index-단일파일.html`

---

## 확인할 것

- **입장이 되는가** (필명 + 비밀번호 6자 이상)
- **같은 필명에 다른 비밀번호로 들어가면 막히는가** ← 이게 이번의 핵심입니다
- **🪟 공용 뽀모** 버튼이 새 창을 여는가
- 채팅이 오가는가 (다른 창을 하나 더 열어 다른 필명으로 들어가 보세요)
- 카드 아래 상자를 누르면 **내 카드는 편집**, **남의 카드는 기록**이 뜨는가
- 주소창 오른쪽에 설치 아이콘 **⊕** 가 뜨는가

벨사탕 방에 들어가 봐서 **TheMagam 의 접속자가 안 보이면** 분리가 제대로 된 것입니다.
